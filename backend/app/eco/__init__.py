"""Ecosystem exposure against event footprints.

Reference layers live under backend/data/eco/. Computation runs in the worker
after a footprint is written; the API serves the cached result only.
"""
